<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/postNav"
    tools:ignore="UnusedNavigation">
    <fragment
        android:id="@+id/postNav"
        android:name="com.proyectoclase.retrofitrobert.fragments.fragmentPost"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_post" >
        <action
            android:id="@+id/action_outside_login_self"
            app:destination="@id/postNav" />
    </fragment>
    <fragment
        android:id="@+id/nav_comments"
        android:name="com.proyectoclase.retrofitrobert.fragments.fragmentComment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_commnents" />

</navigation>